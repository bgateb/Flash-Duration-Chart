# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=flashduration
MYSQL_PASSWORD=change-me
MYSQL_DATABASE=flashduration

# Admin (plain password, single author)
ADMIN_PASSWORD=change-me

# iron-session cookie signing secret — must be >= 32 chars
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=replace-with-32-plus-random-chars-replace-me
